/*
variavel que receve uma função

*/
const add = function(a, b) {
    return a + b
}

const subtract = function(a, b) {
    return a - b
}

const multiply = (x, y) => x * y

const divide = (x, y) => x/ y

console.log(add(4,6))
console.log(subtract(10, 4))
console.log(multiply(2, 4))
console.log(divide(8,2))
